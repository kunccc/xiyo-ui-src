import React from 'react';

const Start: React.FC = () => {
  return (
    <article className="markdown-body">
      <h1>开始使用</h1>
      <p>请先<a href="#/install">安装</a>本组件库。</p>
      <p>然后引入组件。</p>
      <pre><code>{`import {Icon, Button, Layout, Dialog} from "xiyo-ui-react"`}</code></pre>
      <p>接下来就可以使用我提供的组件了。</p>
      <h2>React 单文件组件</h2>
      <p>代码示例：</p>
      <pre>
      </pre>
      <p>下一节：<a href="#/start">开始使用</a></p>
    </article>
  );
};

export default Start;